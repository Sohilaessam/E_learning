<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.e_learing">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.E_LEARING"
        android:usesCleartextTraffic="true">
        <activity android:name=".ScoreActivity"></activity>
        <activity android:name=".SingleExam" />
        <activity android:name=".AskquestionActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".studentprofile" />
        <activity android:name=".socreActivity" />
        <activity android:name=".alllectureActivity" />
        <activity android:name=".AllExamActivity" />
        <activity android:name=".allquestion" />
        <activity android:name=".lectureActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RegisterActivity"
            android:exported="true" />
    </application>

</manifest>